spring:
  datasource:
    url: jdbc:mariadb://localhost:${mariaDB4j.port}/test?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: org.mariadb.jdbc.Driver

mariaDB4j:
  port: 3307
